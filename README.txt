First of all install node.js and npm and then:

// install all package for App
1. npm install

// run JSON Server (http://localhost:3000/transactions)
2. npm run server

// run tailwind css
3. npm run dev